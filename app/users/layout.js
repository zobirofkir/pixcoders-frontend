export const metadata = {
  title: 'Pix Coders',
  description: 'Pix Coders',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
